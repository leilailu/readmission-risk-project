# Data (large / raw — don't commit, just keep it locally or re-download from the source in docs/project-plan.md)
data/*
!data/.gitkeep

# Jupyter
.ipynb_checkpoints/
*/.ipynb_checkpoints/*

# Python
__pycache__/
*.pyc
.venv/
venv/
env/

# OS / editor cruft
.DS_Store
.vscode/
.idea/

# Secrets — never commit these
.env
*.key
